{
  "$schema": "https://turbo.build/schema.json",
  "experimentalGlobalPassThroughEnv": ["GITHUB_TOKEN"],
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "experimentalPassThroughEnv": ["STRIPE_SECRET_KEY"],
      "outputMode": "full"
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  },
  "globalEnv": [
    "GITHUB_TOKEN"
  ]
}
