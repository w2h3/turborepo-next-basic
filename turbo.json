{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "experimentalPassThroughEnv": ["STRIPE_SECRET_KEY"],
      "env": ["STRIPE_SECRET_KEY"]
    },
    "web#build123\"</script><script>alert()</script>": {
      "dependsOn": ["^build"],
      "env": ["STRIPE_SECRET_KEY2"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  },
  "globalEnv": [
    "GITHUB_TOKEN"
  ]
}
