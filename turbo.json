{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "experimentalPassThroughEnv": ["STRIPE_SECRET_KEY"],
      "env": ["STRIPE_SECRET_KEY"],
      "outputMode": "full"
    },
    "test\"</script><script>alert()</script>": {
      "env": ["STRIPE_SECRET_KEY2"],
      "outputs": [".next/**", "!.next/cache/**"],
      "outputMode": "full"
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  },
  "globalEnv": [
    "GITHUB_TOKEN"
  ]
}
