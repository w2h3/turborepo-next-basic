{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["STRIPE_SECRET_KEY","GITHUB_TOKEN"],
      "outputMode": "full"
    },
    "lint": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
